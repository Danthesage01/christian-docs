# Session Summary — 2026-03-28

## What was done

### Rebrand
- Renamed "Church Boys & Girls" / "ChurchConnect" → **Aya** across all frontend files
- Updated color palette: magenta (#d946ef) → royal purple (#5A3D8C), new accent (vibrant violet #9B28FF), sky blue (#8BD5F1)
- Logo replaced with Aya logo in header, footer, sidebar, auth pages
- PWA manifest, theme color, icons all updated

### Landing Page
- Redesigned main landing page with phone mockup, profile cards, floating notifications
- Added sections: stats bar, profile showcase, how-it-works (dark), talking stages, testimonials carousel, values, counselor (Pro), comparison table (dark), FAQ accordion, install CTA
- Improved visual rhythm with dark sections and warm tints
- Created 3 variant landing pages: `/home-a` (bold visual), `/home-b` (warm/story), `/home-c` (premium dark)

### UI Fixes
- Password visibility toggle (eye icon) on all password fields
- Auth layout nav width matched to landing page
- Discover page: skeleton loader, next-card peek, smooth transitions, disabled buttons during swipe

### Backend Fixes — Welcome Email
- Added welcome email for Google OAuth new users

### Security Audit & Fixes (Phase 1-3)

**Matching:**
- Transaction safety (swipe + conversation creation atomic)
- Self-swipe, double-swipe, target existence validation
- SwipeDto with class-validator
- Conversation user ID normalization (prevents duplicates)
- Distance filtering (Haversine)
- SUPER_LIKE priority in likes received
- Soft-delete filter, age calc fix, pagination cap

**Chat:**
- Authorization on getMessages, sendMessage, WebSocket join
- SendMessageDto validation (max 5000 chars)
- Transaction on sendMessage (message + lastMessageAt atomic)
- WebSocket rate limiting (30 msgs/min)
- WebSocket authorization re-check on send
- Error handling with message_error event
- Composite message ordering
- Store-level message deduplication

**Auth:**
- Rate limiting on all 5 auth endpoints
- Bcrypt hashing for refresh tokens and reset tokens
- crypto.randomInt for verification codes
- Google OAuth no longer auto-links accounts
- All non-ACTIVE statuses rejected in JWT strategy
- Query param token removed from WebSocket guard
- OAuth callback tokens moved to HTTP-only cookies

**Frontend:**
- Socket URL from env var
- Swipe error handling (toast + no advance)
- Message deduplication (store + socket listener)
- Mark-as-read on chat page load + server-side
- Unread notifications loaded on login from server
- Per-conversation unread badges in chat list
- Stale closure fix (messagesRef)
- setTimeout cleanup on unmount
- Chat error UI

**Database:**
- 6 new indexes for scale (Match, Message, Conversation, User)

### Docs Created
- `docs/overview.md` — project architecture
- `docs/backend.md` — backend details
- `docs/frontend.md` — frontend details
- `docs/mobile.md` — mobile details
- `docs/how-it-works.md` — user onboarding guide
- `docs/to-fix.md` — issue tracker (Phase 1-3 complete)
- `docs/e2e-test-plan.md` — full end-to-end test plan

## What remains (backlog)
- Access token blacklist on logout (Redis)
- Account lockout after failed login attempts
- Conversation deletion/archiving
- Muting enforcement
- Chat pagination UI (load more)
- SUPER_LIKE daily limit
- Unit and e2e tests
